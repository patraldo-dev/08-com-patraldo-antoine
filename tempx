/* Adjust Hero Section */
.hero-simple {
  /* Reduce the overall height of the hero section */
  min-height: 60vh; /* Changed from 70vh */
  padding-top: 4rem; /* Add some top padding so it's not flush with the nav */
}

.hero-content {
  /* Reduce the max-width to prevent text from being too wide */
  max-width: 80%;
  /* Ensure text scales down on smaller screens */
  font-size: clamp(1.5rem, 5vw, 3rem); 
}

.hero-content h1 {
  /* Reduce the font size slightly for better fit */
  font-size: 2.5rem;
  line-height: 1.2;
}

.hero-content .subtitle {
  /* Reduce subtitle font size */
  font-size: 1.2rem;
  margin-top: 1rem;
}

/* Adjust Sketchbook Section */
.sketchbook-section {
  /* Move the sketchbook up by reducing its top margin/padding */
  margin-top: -2rem; /* Negative margin pulls it up */
  padding: 2rem 1rem; /* Or adjust padding as needed */
}
